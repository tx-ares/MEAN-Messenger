# The app folder has modules separated by feature.
# So, the /auth will contain all template, model, and module files for authorization such as 'user' files.
# /messages will contain all related files as above for message modules.
